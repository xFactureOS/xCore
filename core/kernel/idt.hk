def idt_set_gate($num,$base,$sel,$flags) {
xcore = new xCore();    
$xcore->idtgate($num,$base,$sel,$flags);
}
def idt_load() {
xcore = new xCore();    
$xcore->idtload();
}
def idt_install() {
idt_load();
}